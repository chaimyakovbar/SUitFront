# Bug Fixes — 2026-04-30

## 1. Missing translation key: `sizes`
**קובץ:** `src/context/LanguageContext.jsx`

המפתח `sizes` שימש ב-`Shopping.jsx` אך לא היה קיים בתרגומים.

```js
// נוסף ל-he:
sizes: "מידות",

// נוסף ל-en:
sizes: "Sizes",
```

---

## 2. setState בתוך setState — warning ב-console
**קובץ:** `src/components/GetAllSuitFromData.jsx` — פונקציה `handleSelect`

**הבעיה:** `setTotalPrice` נקרא בתוך ה-updater function של `setSelectedSuits`.
React מזהה את זה כ-setState בזמן render ומשליך warning:
> Cannot update a component (Payed) while rendering a different component (DynamicImage)

**לפני:**
```js
const handleSelect = (suitId, price) => {
  setSelectedSuits((prev) => {
    const newSet = new Set(prev);
    if (newSet.has(suitId)) {
      newSet.delete(suitId);
      setTotalPrice((prevPrice) => prevPrice - price); // ← בתוך updater!
    } else {
      newSet.add(suitId);
      setTotalPrice((prevPrice) => prevPrice + price); // ← בתוך updater!
    }
    return newSet;
  });
};
```

**אחרי:**
```js
const handleSelect = (suitId, price) => {
  const isSelected = selectedSuits.has(suitId); // קרא state לפני
  setSelectedSuits((prev) => {
    const newSet = new Set(prev);
    if (newSet.has(suitId)) newSet.delete(suitId);
    else newSet.add(suitId);
    if (onSelect) onSelect(suitId);
    return newSet;
  });
  setTotalPrice((prev) => isSelected ? prev - price : prev + price); // בנפרד
};
```

---

## 3. מחיקת חליפה לא ניקתה את ה-selected state
**קובץ:** `src/components/GetAllSuitFromData.jsx` — פונקציה `handleDelete`

**הבעיה:** כשמשתמש מחק חליפה שהייתה מסומנת (checkbox), המחיר הכולל נשאר גבוה וה-state התבלבל.

**תיקון:** לאחר מחיקה מוצלחת, בודקים אם החליפה הייתה ב-selectedSuits ומסירים אותה:
```js
if (selectedSuits.has(suitId)) {
  setSelectedSuits((prev) => {
    const next = new Set(prev);
    next.delete(suitId);
    return next;
  });
  setTotalPrice((prev) => prev - (suit?.totalPrice || 0));
}
```

---

## 4. מחיר — שינוי ל-₪1,500
**קובץ:** `src/config/suitPricing.js`

```js
// לפני:
basePrices: {
  kind1: 500,
  kind2: 550,
  kind3: 600,
  kind4: 650,
}

// אחרי:
basePrices: {
  kind1: 1500,
  kind2: 1600,
  kind3: 1700,
  kind4: 1800,
}
```

**קובץ:** `src/components/GetAllSuitFromData.jsx` — תצוגת מחיר בכרטיס

```js
// לפני:
{item.totalPrice}$

// אחרי:
₪{item.totalPrice?.toLocaleString()}
```
